# `stat_arb` package placeholder

This directory is intentionally empty of Python modules. Production code will
be introduced only when the first Research V2 behavior slice begins.

The intended ownership and dependency rules are defined in
`ARCHITECTURE_REFACTOR.md`. Canonical and temporary module documentation is
indexed in `docs/index.md`.

The permanent module contracts cover Research, Pairs, Market Data, Exchange,
Trading, Operations, Interfaces, and Core. Their physical packages are created
only when an implementation slice requires concrete behavior.
