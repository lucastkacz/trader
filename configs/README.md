# V2 configuration

No V2 operational configuration exists yet.

Configuration files will be introduced only after their typed models and
validation behavior exist. Raw YAML dictionaries must not pass below the config
boundary, and operational values must not be hidden in domain code.
