# V2 tests

No V2 tests exist yet because production implementation has not started.

Tests will be added with behavior slices and will target public module
interfaces. The default suite must be deterministic and must not depend on
uncontrolled external availability. Online and Demo integration suites are
explicitly marked and selected when their implementation gate requires them.
